<!-- docs/slices/00. Network POC Spike.spec.md -->

# Slice Spec: Network POC (Spike)

| | |
|---|---|
| **Doc Type** | Slice Spec |
| **Author** | Gregory McQuillan |
| **Date** | 2026-09-06 |
| **Status** | Complete |
| **Reviewers** | — (solo project) |

**Implements:** `01. Architecture Overview.edd.md`, Testing & Rollout Plan step 0
— a spike, not a shippable vertical slice.
**TL;DR:** Prove desktop mDNS-advertise + iOS mDNS-discover + a two-way WebSocket
message exchange works on a real LAN across two physical devices, prove the
protobuf toolchain works across Rust/Swift, and prove a QR code can be rendered
from a Tauri-less Rust CLI — all throwaway, kept in the repo afterward only as an
unmaintained reference.

---

## Scope

**In:**
- **Desktop** (standalone `cargo run` binary, no Tauri):
  - mDNS-advertises a service using `_buttonspoc._tcp.local` — deliberately
    distinct from the EDD's real service type (§5.1, `_buttons._tcp.local`) so
    this throwaway spike is never mistaken for, or collides with, the real
    product's future mDNS presence on the same LAN. Otherwise mirrors the EDD's
    approach for realism.
  - Renders a QR code to the terminal (Unicode block QR) encoding a placeholder
    payload (device ID + address) — proves the generation technique in isolation,
    not wired to an actual scan-to-pair flow in this POC.
  - Runs a minimal WebSocket server. Once a client connects, sends it a `Ping`.
  - Logs to stdout whatever `Ping` it receives back from the client.
- **iOS** (minimal SwiftUI app):
  - Local Network permission handling (`NSLocalNetworkUsageDescription` +
    `NSBonjourServices` in Info.plist) — confirming this flow actually works is
    itself part of what's being tested.
  - Discovers the desktop via `NWBrowser` (no manual IP entry).
  - A `Text` view displaying the `text` field of the `Ping` received from
    desktop.
  - One `Button`, **disabled** until that `Ping` arrives.
  - On tap, sends a `Ping` back whose `text` is `"Reply To: <original
    message>"`, substituting the desktop's original message text.
- **Protobuf toolchain:** a single trivial message, `Ping { text: string }`, in
  `/spikes/network-poc/proto/`, generated for both Rust (`prost`) and Swift
  (`swift-protobuf`) and sent as WebSocket binary frames. This is a throwaway
  message purely to prove the codegen/build toolchain works end-to-end across
  both languages — it is **not** the real schema, which still waits for EDD
  roadmap step 6, informed by the UI work in steps 2-5.

This exercises the channel in both directions — desktop→mobile (enable-style
`Ping`) and mobile→desktop (button-press-style `Ping`) — mirroring the real
protocol's `config_sync` and `button_press` roles (EDD §5.2) without needing the
real schema.

**Out:**
- The real button/grid/Profile schema — only a throwaway `Ping` message exists
  here.
- QR *scanning* on iOS — generation is tested standalone in this POC.
- Real pairing/auth/token exchange.
- Tauri, or any UI beyond the one button.
- Reconnect logic, retries, or error handling beyond what's needed to observe
  success or failure.
- Persistence of any kind.

> **Rule for agent:** stay inside this scope. If something out of scope blocks
> you, stop and flag it — don't silently expand scope to route around it.
>
> **This is throwaway code, but not deleted.** It lives entirely in
> `/spikes/network-poc/`, physically separate from `/desktop` and `/ios` (which
> don't exist yet — this POC runs before Slice 1). It stays in the repo
> afterward as an unmaintained reference — mark it clearly (e.g. a top-level
> comment/README noting it's a spike, not part of the shipped product) so it's
> never mistaken for real product code or wired into the real Slice 1 scaffold.

## Files to Touch

- `/spikes/network-poc/desktop/` — create — standalone Cargo project
  (`mdns-sd`, `tokio`, `tokio-tungstenite`, `qrcode`, `prost` crates).
- `/spikes/network-poc/ios/` — create — minimal Xcode SwiftUI project, with the
  `swift-protobuf` plugin configured for codegen.
- `/spikes/network-poc/proto/ping.proto` — create — the single throwaway
  message definition.

> **Rule for agent:** don't touch files outside this list without flagging it
> first. Do not touch `/desktop`, `/ios`, `/android`, or `/protocol` — they
> don't exist yet at this point in the roadmap.

## Interface / Data Contract

```protobuf
// spikes/network-poc/proto/ping.proto — throwaway, not the real schema
syntax = "proto3";
message Ping {
  string text = 1;
}
```

| Message | Direction | Payload |
|---|---|---|
| (mDNS advertisement) | D→M | Service type `_buttonspoc._tcp.local`, instance name |
| (QR, terminal-only) | — | Placeholder string: device ID + `IP:port` |
| `Ping` | D→M, WebSocket binary frame | Sent once on connect; `text` is shown in the iOS `Text` view and enables the button |
| `Ping` | M→D, WebSocket binary frame | Sent on button tap; `text` is `"Reply To: <desktop's original message>"` |

## Implementation Notes

- iOS: `Network` framework's `NWBrowser` for discovery, `URLSessionWebSocketTask`
  for the connection — the same APIs the real EDD design calls for (§5.1/5.2), so
  this POC genuinely tests the intended approach, not a shortcut.
- Rust QR rendering: `qrcode` crate's Unicode renderer
  (`render::unicode::Dense1x2`) prints a scannable code directly to stdout — no
  image viewer needed.
- Protobuf codegen: `prost-build` in the desktop's `build.rs`; the
  `swift-protobuf` Xcode plugin (or a manual `protoc --swift_out` build step) on
  iOS. Send `Ping` messages as WebSocket **binary** frames, not text — confirm
  both `tokio-tungstenite` and `URLSessionWebSocketTask` handle binary frames
  correctly, since the real protocol will need this too.
- Confirm the iOS Local Network permission prompt appears and that discovery
  proceeds correctly once granted.
- If anything here surprises you (permission quirks, mDNS flakiness, protobuf
  toolchain friction), note it — it likely needs to feed back into EDD §5.1/5.2
  before Slice 1 starts.

## Definition of Done

- [x] Desktop binary runs, advertises via mDNS, and prints a terminal QR code.
- [x] `Ping` compiles and generates working types on both Rust and Swift.
- [x] iOS app discovers the desktop service via mDNS — no manual IP entry.
- [x] iOS Local Network permission prompt appears; once granted, discovery and
      connection proceed.
- [x] On WebSocket connect, desktop sends a `Ping`; its `text` appears in the
      iOS `Text` view and the button becomes tappable.
- [x] Tapping the button sends a `Ping` with `text` = `"Reply To: <original
      message>"`, which the desktop visibly logs.
- [x] Confirmed on **two real physical devices** on the same LAN — not two
      processes on one machine. That's the actual point of this POC.

**Verification:**
```
# terminal 1
cd spikes/network-poc/desktop && cargo run
# then run the iOS app on a physical device on the same LAN,
# confirm the button enables, tap it, confirm the desktop logs the message
```

> **Rule for agent:** run the verification above on real hardware before
> declaring this done. Report the actual observed output, not an assumption
> that it passed.

## Rollback

N/A — spike. Not deleted regardless of outcome: success moves straight to
Slice 1; failure means findings get folded into the EDD before Slice 1 starts.
Either way, `/spikes/network-poc/` stays in the repo afterward as an
unmaintained reference, clearly marked as such.
