<!-- docs/slices/00b. egui Desktop UI Spike.spec.md -->

# Slice Spec: egui Desktop UI Spike

| | |
|---|---|
| **Doc Type** | Slice Spec |
| **Author** | Gregory McQuillan |
| **Date** | 2026-09-07 |
| **Status** | Complete |
| **Reviewers** | — (solo project) |

**Implements:** `01. Architecture Overview.edd.md` §4 ("Why Tauri over Qt/C++")
— a spike revisiting that call, not a roadmap step. Not shippable.
**TL;DR:** Rebuild slice 2's exact static button grid in `egui`/`eframe`
(standalone, no Tauri) to compare against the current Tauri+Svelte approach on
the three things that actually matter for a background tool streamers run
while gaming: responsive/reorderable grid layout effort, custom-widget
drawing, and idle CPU/GPU footprint — measured, not assumed.

---

## Scope

**In:**
1. Standalone `cargo run` binary (`eframe`/`egui`), no Tauri, in
   `/spikes/egui-desktop-poc/`.
2. Rebuild slice 2's mock grid: same 8-entry mock data shape (`id`, `label`,
   `icon` as emoji/text glyph), rendered as icon+label tiles.
3. Responsive wrap: grid re-flows sensibly across a few window widths — same
   comparison points as slice 2's screenshots (~1200px and ~500px).
4. Drag-to-reorder buttons.
5. A **custom-painted** button tile (via `egui::Painter`/`Ui`, not a bare
   default `egui::Button`) — proving hand-drawn widget styling is comfortable,
   since that's part of egui's appeal per the discussion that motivated this
   spike.
6. Manual repaint/frame throttling: app must not continuously redraw when
   idle/backgrounded — explicitly configured and measured, not assumed from
   egui's defaults (see Implementation Notes).
7. Measured idle CPU/GPU comparison: this spike's binary vs. the slice-2 Tauri
   app, both minimized/backgrounded, over the same time window.

**Out:**
1. Real persistence, actions, or Tauri IPC — slice 3's actual feature work is
   untouched; this is a UI-capability/perf comparison only.
2. Any commitment to switch away from Tauri — this spike informs a decision, it
   doesn't make one.
3. Cross-platform testing beyond this dev machine (macOS) — Windows/Linux idle
   behavior (esp. WebView2 vs `eframe`'s `winit`/`wgpu` backend) is a real gap
   this spike can't close; flag it as an open question, don't guess at it.
4. Networking, mDNS, protobuf — irrelevant here.

> **Rule for agent:** stay inside this scope. If something out of scope blocks
> you, stop and flag it — don't silently expand scope to route around it.
>
> **This is throwaway code, but not deleted.** Lives entirely in
> `/spikes/egui-desktop-poc/`, physically separate from `/desktop`. Stays in
> the repo afterward as an unmaintained reference regardless of outcome, same
> convention as `/spikes/network-poc/` — mark it clearly as a spike.

## Files to Touch

1. `/spikes/egui-desktop-poc/Cargo.toml` — create — `eframe`, `egui` deps.
2. `/spikes/egui-desktop-poc/src/main.rs` — create — app entrypoint, repaint
   throttling setup.
3. `/spikes/egui-desktop-poc/src/button_grid.rs` — create — responsive grid +
   drag-reorder + custom-painted tile widget.
4. `/spikes/egui-desktop-poc/src/mock_buttons.rs` — create — same mock data
   shape as `/desktop/src/lib/data/mockButtons.ts` (mirrored by hand, not
   shared code — this is a standalone throwaway binary).
5. `/spikes/egui-desktop-poc/README.md` — create — spike marker note, same
   convention as `/spikes/network-poc/README.md`.

> **Rule for agent:** don't touch files outside this list without flagging it
> first. Do not touch `/desktop` or any of slice 3's in-progress work.

## Interface / Data Contract

```rust
// spikes/egui-desktop-poc/src/mock_buttons.rs — mirrors desktop's MockButton,
// hand-copied on purpose, not shared code (throwaway spike)
struct MockButton {
    id: String,
    label: Option<String>,
    icon: Option<String>, // emoji/text glyph, same convention as slice 2
}
```

No serialization boundary exists in this spike (no persistence, no IPC) —
plain in-memory `Vec<MockButton>`.

## Implementation Notes

1. **Repaint throttling is not eframe's default** — by default `eframe`/`egui`
   can redraw continuously. Must explicitly use `egui::Context::request_repaint()`
   / `request_repaint_after()` (event-driven redraw only) rather than assuming
   reactive behavior for free. This is the exact claim under test — confirm it,
   don't take it on faith.
2. `egui::Grid` is fixed-rows/cols, not CSS `auto-fill`-style responsive wrap —
   the responsive layout will need to be hand-rolled (compute columns from
   available width each frame). This is the real unknown the spike exists to
   surface, not a formality.
3. Idle CPU/GPU measurement (macOS): `powermetrics` (`sudo powermetrics
   --samplers cpu_power,gpu_power -i 1000 -n 30`) or Activity Monitor's
   Energy/GPU History tabs. Take the same measurement, same duration, on the
   slice-2 Tauri app (`npm run tauri dev`, window minimized) for a fair
   comparison — write both numbers down, not just this spike's.
4. Whatever this spike finds — either direction — should feed back into EDD §4
   (`Why Tauri over Qt/C++`) as either reinforcement or a revision, same as the
   network POC spike's surprises were meant to feed back into §5.1/5.2.

## Definition of Done

1. [x] App renders the same 8 mock buttons (icon+label) as slice 2.
2. [x] Grid wraps responsively at ~1200px and ~500px window widths — screenshot
       both, same widths as slice 2's Playwright screenshots, for a direct
       side-by-side comparison. Confirmed live-resizing down to ~500px; formal
       side-by-side screenshots still pending.
3. [x] Buttons reorder via drag-and-drop. Live swap-on-hover-during-drag, plus
       a cursor-following ghost (floating `egui::Area`) — confirmed working
       well by hand.
4. [x] Button tile is a custom-painted widget, not a default `egui::Button`.
5. [x] Idle/backgrounded **CPU and GPU** usage measured over the same window
       as the slice-2 Tauri app — CPU via `ps` (agent), GPU via `sudo
       powermetrics` (user, ran directly — no TTY for the password prompt in
       the agent's shell). Both show zero measurable delta between either app
       and a do-nothing baseline. See Findings.
6. [x] Written comparison covering: dev experience (lines of code / time spent
       on responsive wrap + drag-reorder vs. slice 2's CSS), and the measured
       idle numbers. See Findings' closing entry and Decision below.

## Decision

**Proceed with Tauri/Svelte for now.** Summary of what this spike settled:

1. **Idle CPU/GPU: a wash.** Both frameworks measure identically once
   measurement conditions are actually controlled (see Findings) — zero
   measurable resource cost from either at rest. The original motivating
   worry (background tool draining resources during gameplay) isn't a reason
   to pick one over the other, at least not for idle-in-tray behavior.
2. **Dev experience: Tauri/CSS wins on velocity for this app's UI-heavy
   surface area.** Responsive grid wrap, theming, DnD ghost styling, emoji
   rendering — all free or near-free in CSS/web, all needed hand-rolling in
   egui (small individually, ~15-50 lines each, but it adds up across every
   UI feature this app needs: folders, pages, Profiles, icon editing, still
   ahead). Matches the product's actual priority right now: shipping and
   testing with real users, not framework purity.
3. **egui's real strength — full control once built — isn't needed yet.**
   The drag-ghost work showed this clearly: more upfront effort than HTML5
   DnD, but zero fighting a framework default afterward (vs. Tauri needing
   `dragDropEnabled: false` to even let the browser's own DnD events fire).
   Worth revisiting if/when Tauri's webview-specific quirks become a
   recurring tax, not before.
4. **Real-world precedent for web-rendered always-on-alongside-a-game
   tools:** Battle.net, the Riot Client, and Steam's client UI all run web-
   rendered UI (CEF-based) simultaneously with games, at scale, for exactly
   this "background utility while gaming" use case. Doesn't prove Tauri's
   lighter OS-native-webview approach behaves identically to a full bundled
   CEF, but it's real evidence this general category of tool is viable built
   on web tech, not just a hopeful assumption.
5. **Migration path stays open, deliberately.** This session also decoupled
   `config.rs` from Tauri (`&Path` instead of `&AppHandle`) specifically so
   the Rust core — data model, persistence, and `actions.rs` next — stays
   reusable if a future Tauri→egui migration is ever needed. Per the
   migration-lift discussion: if that discipline holds through `actions.rs`
   and the eventual protocol/networking code, a later migration is a UI-only
   rewrite (Svelte frontend + Tauri glue swapped for egui), not a rebuild.

`/spikes/egui-desktop-poc/` stays in the repo as a working reference for that
future migration, per the Rollback section below — proven functional (grid,
responsive wrap, custom-painted tiles, drag-reorder with ghost), not just a
boot-check stub.

**Verification:**
```
cd spikes/egui-desktop-poc && cargo run
```
Resize across the two comparison widths and screenshot. Drag to reorder.
Minimize/background the window and take the `powermetrics` measurement above;
repeat the same measurement against the slice-2 Tauri app for comparison.

> **Rule for agent:** run the verification above and record actual measured
> numbers before declaring this done — don't assume egui's idle behavior,
> confirm it.

## Findings

*(running log, filled in as the spike proceeds)*

1. **Idle CPU: flat 0% for egui, matching Tauri — first reading was a
   measurement artifact, corrected after a controlled re-test.** Measured via
   `ps -o %cpu= -p <pid>`, 10 samples @ 1s.

   First pass (`Sense::click_and_drag()`, as shipped) showed a surprising
   sustained ~11.5% avg with no decay, vs. Tauri's decaying-to-0.0% pattern.
   Isolating by swapping only the `Sense` argument (`hover()` → 0%, `click()`
   → 0%, `click_and_drag()` → ~11%) initially pinned it on drag sensing
   specifically — but the user flagged they'd incidentally interacted with
   the window during that first egui run. Re-ran `click_and_drag()` with the
   machine deliberately left untouched: **flat 0.0%, all 10 samples** —
   identical to Tauri and to the earlier hover/click isolation runs. Separately,
   the user also flagged that the Tauri baseline's early non-zero samples came
   from them dragging the window to see the terminal underneath, not from app
   startup cost as first assumed — so that decay was interaction-driven too,
   not a Tauri-specific transient.

   | Build / condition | Samples (%CPU) | Pattern |
   |---|---|---|
   | Tauri (slice 3, `target/debug/desktop`) — window was dragged mid-sample | 7.5, 3.4, 1.3, 0, 0, 0, 0, 0, 0, 0 | Decays to flat **0.0%** once interaction stops |
   | egui, `click_and_drag()`, first run (machine not confirmed idle) | 12.2, 8.7, 10.0, 12.5, 4.0, 11.9, 14.3, 18.9, 11.2, 11.7 | Sustained ~11.5% — **artifact, not reproduced** |
   | egui, `Sense::hover()` (isolation) | flat **0.0%** ×10 | — |
   | egui, `Sense::click()` (isolation) | flat **0.0%** ×10 | — |
   | egui, `click_and_drag()`, re-run (machine confirmed idle) | flat **0.0%** ×10 | Matches Tauri |

   **Conclusion:** no real idle-CPU penalty from egui's drag sensing —
   `eframe-0.29.1` really does implement proper `ControlFlow::Wait`/
   `WaitUntil` reactive repaint (confirmed in its own source,
   `native/run.rs`) and it works as advertised once the measurement is
   actually controlled. Both apps behave identically and correctly: **real
   interaction (window drag, mouse activity) causes visible CPU draw in
   either framework, and both settle to flat 0% once genuinely left alone.**
   Not a framework difference — a measurement-methodology lesson (confirm the
   machine is actually idle before sampling) worth remembering for any future
   perf comparison here.

   **GPU, measured by the user directly** (`sudo powermetrics --samplers
   gpu_power -i 1000 -n 15`, 15 samples @ 1s, machine confirmed idle for all
   three runs — a do-nothing baseline, egui running, Tauri running):

   | Condition | GPU active residency (avg of 15) | GPU Power (avg of 15) |
   |---|---|---|
   | Baseline (nothing test-related; Docker + browser windows already running) | 42.95% | 619.5 mW |
   | egui app, idle | 42.95% | 627.5 mW |
   | Tauri app, idle | 43.07% | 625.0 mW |

   All three statistically indistinguishable — well within the sample-to-
   sample noise seen inside each individual 15-sample run (baseline alone
   ranged 40.1%–49.9% / 520–684 mW). **Zero measurable GPU cost from either
   app at idle** — both sit inside the noise floor of whatever else was
   already running on the machine. Combined with the CPU result: neither
   framework shows any idle resource cost once actually idle. The original
   motivating worry (a background tool eating frames during gameplay) is not
   borne out by either framework *at rest* — the real question, if this ever
   gets tested further, is behavior while the grid UI is actively visible/
   interacted with during a stream, not idle-in-tray behavior.
   **GPU comparison still outstanding** — `powermetrics` needs `sudo` and an
   interactive password prompt neither available in the agent's shell; handed
   to the user to run directly and report back.

2. **Responsive wrap: hand-rolled, worked first try, no major friction.**
   `egui::Grid` has no `auto-fill` equivalent, so column count is computed from
   `ui.available_width()` and rows are ended manually every N items
   (`button_grid.rs`). ~15 lines total. Confirmed live-resizing down to
   ~500px — re-flows correctly. Real effort, but small — not the blocker the
   spec worried it might be.
3. **Emoji icons render with tofu boxes for multi-codepoint glyphs.**
   E.g. `⏺️` (`U+23FA` + `U+FE0F` variation selector) shows a square for the
   selector codepoint — egui's default bundled font has no glyph for it.
   Root cause: browsers/webviews get full OS emoji-font fallback for free;
   egui does not — an emoji font has to be loaded explicitly (`egui_extras`
   has helpers for this). Real Tauri-vs-egui delta for this app's specific
   icon convention (emoji-as-glyph, per slice 2). Left unfixed per decision —
   doesn't block layout/reorder/perf testing, logged as a real cost if egui
   is ever chosen for real.
4. **Drag-and-drop: full manual control, but genuinely nothing free.**
   First pass (swap-in-place on hover) worked but gave weak feedback — no
   visual signal of "a move is happening" beyond a dim/transparent tile, and
   the dim itself was initially invisible (semi-transparent white over a
   near-white panel background — a contrast bug, not a framework issue).
   Adding a cursor-following ghost (`egui::Area` at `Order::Tooltip`, ~15
   lines, reusing the tile-paint function) fixed this and felt good once in.
   Contrast with Tauri: HTML5 drag-and-drop gives a ghost cursor for free,
   but that default look isn't stylable and Tauri's own OS-level drag
   interceptor had to be explicitly disabled (`dragDropEnabled: false`) just
   to let the browser's DnD events fire at all (see slice 3). egui required
   more manual work up front, but every pixel of the drag feedback is fully
   under our control once written — no fighting a framework default.

## Rollback

N/A — spike. Not deleted regardless of outcome: a clear win for egui feeds
back into EDD §4 before slice 3 implementation starts; a wash or a loss means
slice 3 proceeds on Tauri as planned and this stays as a documented, revisited
decision. Either way, `/spikes/egui-desktop-poc/` stays in the repo afterward
as an unmaintained reference, clearly marked as such.
